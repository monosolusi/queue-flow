# Dependencies
node_modules/

# Build output
dist/
build/

# Test coverage
coverage/

# tsc -b composite-project emit artifacts for vite.config.ts (tsconfig.node.json).
# These are regenerated on every build and must not be committed — unlike
# caller-service, the kiosk gitignores them to avoid build-churn in the tree.
vite.config.js
vite.config.d.ts
*.tsbuildinfo

# Logs & OS
*.log
.DS_Store